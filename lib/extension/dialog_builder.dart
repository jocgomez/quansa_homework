import 'package:flutter/material.dart';

/// Interfaz para renderizar diferentes tipos de dialog, en este caso solo
/// el subtipo de todo_dialog.
class DialogBuilder {
  Widget buildAlertDialog(BuildContext context) {
    return const AlertDialog();
  }
}
